/**
 * Firebase configuration placeholder.
 * -----------------------------------
 * 1. Copy this file and rename the copy to `firebase-config.js`.
 * 2. Replace every value below with the configuration snippet you get
 *    from Firebase Console > Project Settings > General > Your apps.
 * 3. Keep `firebase-config.js` out of version control if you don't want
 *    to expose your project keys.
 *
 * The `window.firebaseConfig` object will be picked up automatically
 * by `script.js` during initialization.
 */

window.firebaseConfig = {
    apiKey: "PASTE_FIREBASE_API_KEY",
    authDomain: "PASTE_FIREBASE_AUTH_DOMAIN",
    projectId: "PASTE_FIREBASE_PROJECT_ID",
    storageBucket: "PASTE_FIREBASE_STORAGE_BUCKET",
    messagingSenderId: "PASTE_FIREBASE_SENDER_ID",
    appId: "PASTE_FIREBASE_APP_ID"
};

